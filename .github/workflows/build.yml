name: Build

on:
  push:
    branches:
      - master

jobs:
  build:
    name: Build release zip
    runs-on: ubuntu-latest
    steps:
      # - name: Zip Folder
      #   run: |
      #     cd ../
      #     rm -rf vademecum/vademecum.zip
      #     zip -r vademecum/vademecum.zip vademecum -x "vademecum/.git" -x "vademecum/.github" -x "vademecum/*.zip"
      - uses: actions/upload-artifact@v7
        with:
          name: vademecum_0.2.1.zip
          path: .
